name: light_carousel
description: A Light Carousel package that is forked from carousel pro has been migrated to null safety and some improvements.
version: 0.0.5
homepage: https://github.com/aisivan36/light_carousel

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
